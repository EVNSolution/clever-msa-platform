# Subdomain-First Web Definition Design

## Purpose

이 문서는 `ev-dashboard` 웹 제품을 다시 정의할 때, 메인 도메인보다 **서브도메인 회사 제품을 먼저 정본으로 고정**하기 위한 1차 설계다.

이번 문서의 목적은 아래를 닫는 것이다.

1. 메인 도메인과 서브도메인을 같은 웹으로 볼지, 다른 제품으로 볼지 고정한다.
2. 시스템 관리자와 회사 계정의 세션 경계를 고정한다.
3. 서브도메인 1차 MVP의 정보구조와 deep-link 계약을 고정한다.
4. `배차 -> 근태 -> 자동 정산` 흐름을 웹 기준의 핵심 데이터 파이프라인으로 고정한다.
5. `front-web-console` 하나를 유지하면서도 메인 도메인과 서브도메인이 거의 다른 제품처럼 보이게 하는 원칙을 고정한다.

## Scope

이번 문서는 아래만 다룬다.

- 메인 도메인과 서브도메인의 제품 정의
- 세션/권한 경계
- 서브도메인 1차 MVP의 상단 정보구조
- 서브도메인 대시보드/정산과 정산 내부 메뉴의 역할
- 프론트와 gateway/API 조합 원칙

이번 문서는 아래를 아직 확정하지 않는다.

- 메인 도메인 내부 상세 IA
- 정산 내부 세부 화면의 최종 컴포넌트 설계
- 시각 디자인 시스템 변경
- backend 서비스 경계 재정의

## Problem Statement

현재 웹은 `front-web-console` 하나를 정본으로 유지하고 있지만, 사용자 경험 관점에서는 아래가 아직 명확히 분리돼 있지 않다.

1. 시스템 관리자용 메인 도메인
2. 회사 실무용 서브도메인
3. 회사별 전용 workflow

이 상태를 그대로 두면 아래 문제가 남는다.

1. 시스템 관리자 제품과 회사 실무 제품이 같은 앱의 다른 메뉴처럼 느껴진다.
2. 회사 계정이 자기 회사 전용 시스템을 쓰는 느낌보다 공용 허브에 들어온 느낌이 강하다.
3. 배차 중심 운영과 자동 정산이라는 실제 업무 흐름이 제품 IA의 중심에 오지 못한다.
4. 회사별 복제 확장 시에도 “도메인만 다르고 구조는 공용 허브”라는 애매한 상태가 반복된다.

## Approaches Considered

### 1. 공용 허브 유지 + 메뉴만 분기

메인 도메인과 서브도메인을 사실상 같은 제품으로 두고, 메뉴와 첫 화면 정도만 다르게 두는 방식이다.

장점:

- 구현이 가장 쉽다.
- 기존 공용 레이아웃을 많이 재사용할 수 있다.

단점:

- 회사 전용 제품처럼 느껴지지 않는다.
- 시스템 관리자와 회사 운영자의 제품 경계가 흐려진다.
- 서브도메인 복제 모델에도 맞지 않는다.

이 안은 채택하지 않는다.

### 2. 완전 별도 프론트 제품

메인 도메인과 서브도메인을 repo, 빌드, 배포까지 모두 다른 프론트 제품으로 나누는 방식이다.

장점:

- 사용자 경험 분리가 가장 분명하다.
- 장기적으로는 제품 경계가 가장 선명하다.

단점:

- 현재 `front-web-console` 자산과 세션 구조를 과도하게 다시 나누게 된다.
- 1차 MVP 범위에서 비용이 너무 크다.
- 같은 MSA와 gateway를 쓰는 현재 구조와도 맞지 않는다.

이 안도 1차에서는 채택하지 않는다.

### 3. 단일 프론트 앱 유지 + 도메인별 거의 다른 제품 surface

repo와 앱은 하나로 유지하되, 메인 도메인과 서브도메인은 **서로 다른 shell, 다른 첫 진입, 다른 상단 구조, 다른 페이지 묶음**을 갖게 만드는 방식이다.

장점:

- 현재 구조를 유지하면서도 제품 경험은 분리할 수 있다.
- 회사별 서브도메인 복제 모델과 잘 맞는다.
- gateway와 MSA 경계를 바꾸지 않아도 된다.

단점:

- 프론트 내부에서 제품 경계를 엄격히 지켜야 한다.
- 메인/서브도메인 공용 자산과 제품별 자산의 구분이 필요하다.

이번 문서는 이 안을 채택한다.

## Primary Decision

웹은 계속 `front-web-console` 하나를 canonical frontend source로 유지한다. 하지만 사용자 경험은 아래 두 제품처럼 보이게 한다.

- 메인 도메인 제품
  - 시스템 관리자 전용
- 서브도메인 제품
  - 회사 1개에 고정된 전용 실무 제품

즉 **코드베이스는 하나, 제품 경험은 둘**이라는 원칙을 채택한다.

## Domain Model

### 1. Main Domain

메인 도메인은 시스템 관리자 전용 제품으로 고정한다.

핵심 원칙:

- 시스템 관리자만 메인 도메인 세션을 사용한다.
- 회사 계정은 메인 도메인에서 실무 사용을 하지 않는다.
- 메인 도메인 상단 구조는 1차 기준으로 아래만 고정한다.
  - `대시보드 / 회사 / 정산`
- 단, 메인 도메인 상세 IA는 이번 문서의 우선순위가 아니다.

`회사` 탭의 1차 역할:

- 회사 목록 조회
- 회사 상태 조회

즉 메인 도메인은 1차에서 “회사 운영 제품”이 아니라 “시스템 관리자 허브”로 남는다.

### 2. Subdomain

서브도메인은 회사 1개에 고정된 전용 제품이다.

핵심 원칙:

- 회사 1개 = 서브도메인 1개
- 메인 도메인에서 회사 생성이 곧 서브도메인 생성은 아니다
- 서브도메인/회사 매핑은 시스템 레벨에서 관리되고 DB에 고정된다
- 회사 계정은 자기 회사 서브도메인에서만 세션을 사용한다

예시 문맥:

- 메인 도메인: 시스템 관리자용
- 현재 서브도메인: `CHEONHA`

### 3. Host Resolution Contract

서브도메인 host는 회사 display name으로 즉석 계산하지 않는다. 시스템이 관리하는 **고정 tenant slug**를 기준으로 해석한다.

1차 host 규칙:

- 메인 도메인
  - `ev-dashboard.com`
- 회사 서브도메인
  - `<tenant-slug>.ev-dashboard.com`
- 현재 예시
  - `cheonha.ev-dashboard.com`

원칙:

- `tenant-slug`는 시스템 레벨에서 별도로 관리되는 고정 식별자다
- 회사 생성이 곧 slug 생성 또는 서브도메인 생성은 아니다
- 회사 display name과 slug는 같은 값일 필요가 없다
- 프론트 bootstrap은 현재 host에서 `main-domain`인지 `company-subdomain`인지 먼저 해석한 뒤 shell을 결정한다

1차 failure 동작:

- 알 수 없는 subdomain host
  - 회사 cockpit shell로 진입시키지 않는다
  - tenant not found 상태로 처리한다
- 유효한 host지만 세션의 회사 문맥이 맞지 않는 경우
  - 로그인 또는 workspace bootstrap 단계에서 차단한다
- 메인 도메인에서 회사 계정 실무 진입 시도
  - 메인 허브 실무 surface로 fallback 하지 않고 명시적으로 거부한다

## Session and Identity Rules

### 1. Account Boundary

- 시스템 관리자만 공유 계정 체계를 사용한다.
- 회사 이하 계정은 회사 세션만 허용한다.
- 회사 계정은 자기 회사 도메인에서만 로그인하고 사용한다.

### 2. Login UX

메인 도메인과 서브도메인의 로그인은 공통 로직을 일부 재사용해도, UX는 다르게 보여야 한다.

서브도메인 로그인 원칙:

- 로그인 카드 상단 중앙 헤더에 회사명을 고정 노출한다.
- 사용자는 로그인 시점부터 자기 회사 시스템에 들어왔음을 인지해야 한다.

### 3. Company / Fleet / Driver Boundary

- 플릿은 회사 내부 단위다.
- 서브도메인의 기본 조회 범위는 회사 전체다.
- 필요 시 플릿으로 좁힌다.

배송원과 차량의 1차 경계:

- 회사 간 공유 자원으로 보지 않는다.
- 1차 MVP에서는 `회사 + 배송원`, `회사 + 차량` 문맥으로 본다.
- 같은 사람이 여러 회사에 존재하더라도 시스템상 각 회사의 별도 엔티티로 취급한다.

## Subdomain MVP Information Architecture

서브도메인 1차 MVP의 주 내비게이션은 **항상 노출되는 탑바**가 아니라, 좌상단 회사 브랜드 영역에서 펼쳐지는 **아코디언 메뉴**로 고정한다.

1차 아코디언 상위 메뉴는 아래 2개다.

- `대시보드`
- `정산`

즉 1차 MVP의 canonical subdomain shell은 `대시보드 / 정산` 2축이다.

### 1. Dashboard

서브도메인의 첫 진입 페이지는 회사별 대시보드다.

이 대시보드는 서브도메인의 **유일한 기본 진입점**이다.

즉 1차 구현 계획에서는:

- 서브도메인 첫 진입을 별도 cockpit home으로 두지 않는다
- `대시보드 / 정산` 아코디언 shell이 유일한 canonical subdomain shell이다
- 기존 `cheonha` cockpit 문서는 `정산` 내부 reference policy의 근거로만 남긴다

핵심 섹션:

1. 최근 6개월 수입/지출
2. 금월 배차표 기반 근태
3. 금일 배차

시간축 원칙:

- 기본은 `오늘`
- 상단에서 월 기준 전환 가능

수입/지출 수치 원칙:

- **확정된 결과만** 반영한다
- 잠기지 않은 자동 계산 결과는 6개월 수입/지출 카드에 포함하지 않는다

### 2. Settlement

`정산`은 1차 MVP의 **실제 동작하는 workspace**다. 다만 이 안에서 `배차 -> 근태 반영 -> 자동 정산` 흐름을 함께 가진다.

즉 `배차`는 top-level 메뉴가 아니라 `정산` workspace 내부의 첫 메뉴다.

용어 원칙:

- `배차 업로드`
  - 실제 배차표를 시스템에 넣는 것
- `예상 물량`
  - 별도 개념

이 둘은 이후 문서와 UI에서 섞지 않는다.

입력원 원칙:

- 1차는 기존 배차 업로드 흐름을 따른다
- 구조는 `엑셀 / CSV / 외부 API`로 확장 가능하게 둔다

운영 원칙:

- 기본 정본은 `재업로드`
- 예외적으로 일부 수동 보정 허용

핵심 원칙:

- `정산`은 stub가 아니라 실제 접근 가능한 workspace다
- 단, 실제 write contract를 여는 것은 `정산 처리`와 `스냅샷 잠금` 쪽에 한정한다
- 규칙은 계산을 시작시키는 곳이 아니라 구조를 보여주는 shell이다

1차 MVP에서 제공하는 의미:

- 규칙 설계 화면
- 자동 계산 결과 조회
- 스냅샷 잠금

1차의 `규칙 설계 화면`은 **rule-shell only**로 고정한다.

- `회사 / 플릿 / 배송원` 3단 구조의 자리와 섹션만 제공한다
- 실제 규칙 값은 채우지 않는다
- 1차에서는 rule persistence contract를 열지 않는다
- 저장 버튼, 제출 액션, write API 호출은 없다
- 입력 필드는 실제 편집기가 아니라 placeholder shell 또는 disabled form으로만 표현한다
- 즉 사용자는 “향후 이 단위로 규칙이 들어간다”는 구조만 본다
- 1차 구현 계획에서 이 화면을 실제 저장 가능한 규칙 편집기로 해석하면 안 된다

`확정`의 의미:

- 계산 시작 버튼이 아님
- 이미 존재하는 자동 계산 결과를 특정 시점 기준으로 잠그는 행위

정산 규칙 구조:

- `회사 / 플릿 / 배송원`
- 1차에서는 각 단위의 **틀만 제공**
- 실제 상세 규칙 값과 저장 기능은 후속 단계에서 채운다

### 3. Settlement Internal Menu Structure

`정산` 내부 메뉴와 페이지 이름은 `cheonha` 레퍼런스를 그대로 따른다.

1차 내부 메뉴:

1. `홈`
2. `배차 데이터`
3. `배송원 관리`
4. `운영 현황`
5. `정산 처리`
6. `팀 관리`

이 중 `배차 데이터`가 1차 MVP의 실제 출발점이다.

### 4. Subdomain Route and Deep-Link Contract

서브도메인 route는 아래를 canonical contract로 둔다.

| Surface | Route | Meaning |
| --- | --- | --- |
| 대시보드 | `/` | 회사 대시보드 기본 진입 |
| 정산 기본 | `/settlement` | 정산 workspace 진입. 기본 화면은 `홈` |
| 정산 홈 | `/settlement/home` | `cheonha` 기준 `홈` |
| 배차 데이터 | `/settlement/dispatch` | 배차 업로드, 업로드 결과, 보정의 출발점 |
| 배송원 관리 | `/settlement/crew` | 배송원 관리 |
| 운영 현황 | `/settlement/operations` | 운영 현황 |
| 정산 처리 | `/settlement/process` | 자동 계산 결과와 스냅샷 잠금 |
| 팀 관리 | `/settlement/team` | 팀 관리 |

원칙:

- 서브도메인의 기본 진입은 항상 `/`
- `정산` 상위 메뉴를 열면 내부 deep-link는 위 route를 사용한다
- `배차`는 `/dispatch` 같은 top-level route를 가지지 않는다

### 5. Attendance Surface

`근태`는 1차 MVP에서 독립 메뉴나 독립 route를 가지지 않는다.

근태 노출 위치:

- `대시보드`
  - `금월 배차표 기반 근태` 요약
- `정산 > 홈` 또는 `정산 > 정산 처리`
  - 자동 계산 결과를 설명하는 데이터 문맥의 일부

즉 1차 MVP에서는 근태 데이터가 존재하더라도, 사용자 제품 surface는 아래로 제한한다.

- 대시보드 요약
- 정산 결과/처리 문맥

별도 근태 관리 페이지는 이번 단계의 범위에 넣지 않는다

## Cheonha Reference Policy

서브도메인 제품 전체가 `cheonha`를 그대로 복제하는 것은 아니다.

정확한 적용 범위는 아래와 같다.

- `대시보드`
  - 우리 정의를 따른다
- 상단 탭
  - 우리 정의를 따른다
- `정산`
  - 내부 메뉴와 페이지 구성은 `cheonha` 레퍼런스를 거의 그대로 따른다

즉 레퍼런스 적용 규칙은:

1. 제품 상위 IA는 우리 정의를 우선한다
2. `정산` 내부 메뉴와 페이지 이름은 `cheonha`를 그대로 따른다
3. `배차 데이터` 페이지의 기능은 우리 기존 업로드/자동화 흐름을 따른다
4. backend/API/자동화 기능은 우리 current runtime truth를 따른다

## Core Data Flow

서브도메인 1차 MVP의 핵심 데이터 파이프라인은 아래로 고정한다.

1. `정산 > 배차 데이터`에서 `배차 업로드`
2. `금일 배차 반영`
3. `금월 근태 반영`
4. `정산 초안 생성`

즉 서브도메인 제품의 핵심 가치는 **배차 중심 자동 정산**이다.

### Dispatch-First Automation Rule

`정산 > 배차 데이터`는 단순 파일 업로드 기능이 아니다. 업로드는 아래 자동화의 출발점이어야 한다.

- 배송원 자동 생성
- 날짜 추정
- 플릿 추정
- 근태 생성/반영
- 정산 초안 반영

즉 1차 MVP에서도 사용자는 “배차를 올리면 시스템이 다음 단계를 알아서 이어준다”는 경험을 받아야 한다.

## Frontend / API Relationship

### 1. Frontend Boundary

- 프론트는 계속 `front-web-console` 하나를 사용한다
- 메인 도메인과 서브도메인은 이 앱 안에서 서로 다른 product surface를 가진다

### 2. API Boundary

- API 제품을 별도로 나누지 않는다
- 같은 MSA 서비스 집합을 사용한다
- `edge-api-gateway`가 도메인 문맥에 맞게 호출을 조합한다

즉 차이는 backend 서비스 경계가 아니라 아래에서 만든다.

- shell
- route
- page grouping
- gateway composition

## Error Handling Principles

1차 MVP에서 서브도메인 제품은 아래 실패를 사용자에게 분명히 보여야 한다.

### 1. Wrong Domain / Wrong Company Session

- 자기 회사가 아닌 도메인으로 로그인하려는 경우
- 시스템 관리자 계정으로 서브도메인 실무 진입을 시도하는 경우
- 회사 계정으로 메인 도메인 실무 사용을 시도하는 경우

이 경우는 조용히 fallback 하지 않고, 명시적으로 도메인/권한 불일치를 안내해야 한다.

### 2. Dispatch Upload Failure

- 포맷 오류
- 필요한 열 누락
- 날짜/플릿 추정 실패

이 경우는 업로드 실패, 부분 성공, 자동 생성/추정 결과를 분리해 보여야 한다.

### 3. Settlement Snapshot Lock Failure

- 잠금 대상 스냅샷이 없거나
- 대상 범위가 불완전한 경우

이 경우는 “확정 실패”가 아니라 “잠글 자동 계산 결과가 아직 준비되지 않음”으로 보여야 한다.

## Testing and Verification Guidance

이번 설계를 구현할 때 최소 검증은 아래를 기준으로 둔다.

1. 메인 도메인과 서브도메인이 서로 다른 top-level shell을 가진다
2. 서브도메인 로그인 카드에 회사명 헤더가 고정 노출된다
3. 서브도메인 아코디언 상위 메뉴가 `대시보드 / 정산`으로 고정된다
4. `정산 > 배차 데이터` 업로드 후 `금일 배차 / 금월 근태 / 정산 초안`까지 이어지는 흐름을 검증한다
5. `정산` 내부 메뉴와 페이지 이름이 `cheonha` 레퍼런스 구조와 맞는지 검증한다

## Non-Goals

이번 단계에서 하지 않는 것:

- 메인 도메인 상세 화면을 완전히 설계하는 것
- 회사별 완전 별도 프론트 repo를 만드는 것
- backend 서비스 경계를 다시 자르는 것
- 정산 규칙의 실제 상세 값과 정책 모델을 이번 문서에서 다 채우는 것
- `배차`를 `예상 물량` 중심 화면으로 재정의하는 것

## Relation to Existing Specs

이 문서는 아래 기존 문서의 상위 정본 역할을 가진다.

- [2026-04-15-cheonha-subdomain-cockpit-design.md](./2026-04-15-cheonha-subdomain-cockpit-design.md)

정확한 관계:

- 기존 문서는 `천하운수 cockpit`의 전용 진입과 정산 workspace reference parity를 먼저 닫은 문서다
- 이번 문서는 그 결정을 확장해, **서브도메인 전체 제품 정의를 먼저 정본으로 고정**한다
- 구현 계획은 이번 문서를 우선 기준으로 삼고, `cheonha` 문서는 정산 내부 reference policy의 근거 문서로 남긴다
- 기존 문서의 cockpit home은 이번 문서에서 더 이상 canonical entrypoint가 아니다

## Done Criteria

이번 설계가 닫혔다고 보기 위한 기준:

1. 메인 도메인과 서브도메인의 제품 경계가 구현 계획 수준으로 충분히 명확하다
2. 시스템 관리자 세션과 회사 세션의 경계가 명확하다
3. 서브도메인 1차 MVP의 상단 정보구조가 확정됐다
4. `배차 -> 근태 -> 자동 정산` 흐름이 웹 제품의 핵심 파이프라인으로 명시됐다
5. `cheonha` 레퍼런스를 어디까지 따를지 범위가 명확하다
