# service-support-registry

지원 정본을 위한 empty shell repo다.

현재 역할:
- 문의, 티켓, 응답, 처리 상태 정본 경계를 위한 자리

미래 역할:
- 운영 지원 workflow의 기준 정본
- 이후 support read-model의 upstream

상태:
- runtime 미구현
- 현재는 shell 디렉토리와 README만 존재

정본 문서:
- 플랫폼 아키텍처 정본은 `../../docs/` 아래 문서를 따른다.
